import React from 'react';
import './styles.css';

const Hero: React.FC = () => (
  <div className="hero" id="hero-banner">
    <div className="logo-box">
      <div className="logo" />
    </div>
  </div>
);

export default Hero;
